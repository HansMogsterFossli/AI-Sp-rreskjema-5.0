# Ørebro demo (Web Service) – v2.4 (Light freedom)

Ny tweak:
- Spørsmålene formuleres litt friere/mer naturlig i chatten, men **samme scoring** i bunn.
- Med `CHATAI_API_KEY` blir både oppfølging og spørsmålsformulering AI-drevet.
- Uten nøkkel brukes små variasjoner (templates), så det fortsatt føles mindre rigid.

## Render
- Web Service
- Build: `npm install && npm run build`
- Start: `npm run start`
- Env (valgfritt): `CHATAI_API_KEY`, `CHATAI_BASE_URL`, `CHATAI_MODEL`
