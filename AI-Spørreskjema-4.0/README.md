# Ørebro demo (Web Service) – v2.3 (v4 zip)

Siste tweaks:
- Full bakgrunn (ingen hvit "stripe" nederst)
- Topptekst: "demo utviklet av Hans og Erik"
- Skala/valg-spørsmål har "Ekstra kommentar (valgfritt)" (lagres og vises på lege-siden)
- Fjernet "Vis JSON (for debugging)"

## Render
- Web Service
- Build: `npm install && npm run build`
- Start: `npm run start`
- Env (valgfritt): `CHATAI_API_KEY`, `CHATAI_BASE_URL`, `CHATAI_MODEL`
